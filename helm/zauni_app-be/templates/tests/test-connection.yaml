apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zauni_app-be.fullname" . }}-test-connection"
  labels:
    {{- include "zauni_app-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zauni_app-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
