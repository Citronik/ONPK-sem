apiVersion: v1
kind: Service
metadata:
  name: {{ include "zauni_app-be.fullname" . }}
  labels:
    {{- include "zauni_app-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zauni_app-be.selectorLabels" . | nindent 4 }}
